"$schema": "http://json-schema.org/draft-04/schema#"
title: Calendar
description: calendar object describes user information
type: object
additionalProperties: false
properties:
    accountId:
        description: account id
        type: string
    eventList:
        description: a list of event id
        type: array
        items:
            "$ref": Event.yaml
required:
    - calendarId
    - eventList
